spring:
  datasource:
    url: jdbc:h2:file:./data/coretrain
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  thymeleaf:
    cache: false

app:
  storage:
    dir: ./data/uploads
